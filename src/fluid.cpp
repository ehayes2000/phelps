#include "fluid.hpp"
#include <iostream>

void Fluid::step(float deltaTime){
  // for (auto &p: particles){
  //   p.vel.y -= p.vel.y * deltaTime;
  //   p.vel.y -= deltaTime * GRAVITY;
  // }
}

void Fluid::initializeParticles(){
  // // evenly distribute particles   
  // particles[0] = Particle(5, 20, 0, 0);
  // particles[1] = Particle(5, 30, 0, 0);
  // particles[2] = Particle(5, 40, 0, 0);
  // particles[3] = Particle(5, 50, 0, 0);
  // particles[4] = Particle(5, 100, 0, 0);
}